# AE

This repository contains various PyTorch models for experimentation.

The `neural_operator.py` module includes a minimal implementation of a
1D Fourier Neural Operator (FNO). It can be used to learn mappings
between function spaces using spectral convolutions.
